[package]
name = "midnight-wallet-facade"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "The API a consumer programs a Midnight wallet against, and the implementation over a locally-owned Wallet"

[dependencies]
midnight-wallet = { workspace = true }
midnight-helpers = { workspace = true }
async-trait = { workspace = true }
tokio = { workspace = true, features = ["sync"] }
