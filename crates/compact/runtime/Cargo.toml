[package]
name = "compact-runtime"
description = "Execution-runtime primitives for interpreting Compact circuit IR (runtime values, witnesses, execution results)"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[features]
ledger-9 = ["midnight-types/ledger-9", "midnight-typed-state/ledger-9"]

[dependencies]
midnight-types = { workspace = true }
num-bigint = { workspace = true }
midnight-typed-state = { workspace = true }
midnight-base-crypto = { workspace = true }
compact-codegen = { workspace = true }
thiserror = { workspace = true }
