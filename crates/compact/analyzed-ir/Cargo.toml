[package]
name = "compact-analyzed-ir"
description = "Reader for the Compact compiler's analyzed-ir.sexp artifact: the analyzed IR in its own vocabulary, with ledger operations expanded to Impact VM instructions"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
num-bigint = { workspace = true }

[dev-dependencies]
num-bigint = { workspace = true }
