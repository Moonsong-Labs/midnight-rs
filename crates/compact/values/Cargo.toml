[package]
name = "compact-values"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "The value types generated Compact bindings name. Depends only on crates every ledger generation shares, so a binding's signature does not pin a generation."

[dependencies]
midnight-base-crypto = { workspace = true }
